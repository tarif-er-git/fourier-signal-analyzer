"""Requested signal I/O compatibility location.

Application imports use the top-level ``signal_io`` module because Python's
standard-library ``io`` module is not importable as a project package.
"""
